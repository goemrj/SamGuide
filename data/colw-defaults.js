/* ---------- 열 너비 기본값 ----------
   사이드바 맨 아래 「열 너비 기본값으로 굳히기」를 누르면 이 파일이 다시 써진다.
   손으로 고쳐도 되지만, 굳히기를 한 번 더 누르면 통째로 덮어쓴다.

   왜 파일에 두는가 — 끌어서 바꾼 너비는 원래 localStorage 에만 남아서
   그 브라우저에서만 유효했다. 브라우저 데이터를 지우거나 다른 PC·다른 브라우저에서 열면
   원래대로 돌아갔다. 이 파일은 깃에 올라가므로 **어디서 열든 같은 너비**로 열린다.

   두 겹으로 쓴다 (js/common.js).
     COLW_DEFAULTS  여기 — 소스 기본값. 어느 PC·어느 브라우저에서 열든 이 값으로 열린다.
     samguide_colw  localStorage — 이 브라우저에서만 쓰는 덧쓰기. 있으면 이쪽이 이긴다.
   손잡이를 두 번 누르면 덧쓰기를 지우고 **이 파일의 기본값으로** 돌아간다.
   여기에도 없는 표는 각 page-*.js 에 적힌 % 기본값(BT_ACT_COLW 등)으로 돌아간다.

   모양: {"표이름": [열 너비 px, …], "표이름|w": 표 전체 폭 px}
   표이름은 표가 스스로 달아 둔 data-k 이거나 "화면id#표순서#열수" 다(common.js 의 colwKey).
   아직 굳혀 둔 표가 없어 비어 있다 — 너비를 맞춰 놓고 버튼을 누르면 여기에 채워진다.
------------------------------------------------------------------ */
const COLW_DEFAULTS = {
};
