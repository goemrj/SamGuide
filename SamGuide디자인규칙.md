# SamGuide 디자인 규칙 (팀 공용)

새 화면을 만들거나 기존 화면을 고칠 때, 아래 내용을 **그대로 복사해서 클로드(또는 클로드 코드) 대화 맨 앞에 붙이고** 마지막 줄에 할 일을 적으세요.
Claude Code를 쓰면 이 파일을 저장소 루트에 `CLAUDE.md`로 두면 매번 붙이지 않아도 자동 적용됩니다.

---

## 붙여넣을 프롬프트 (여기서부터 복사)

SamGuide(심사청구 점검 참고자료) 화면을 만듭니다. 아래 디자인 규칙을 **그대로** 지키세요. 규칙에 없는 색·크기를 새로 만들지 마세요.

**팔레트 (이 12개만 사용)**
- 파랑 `#2563EB` (요율·금액·코드값 강조, 활성 상태) / hover `#1D4ED8` / 연한 배경 `#EFF6FF`
- 잉크 `#111827` (제목·항목명) / 본문 `#374151` / 보조 `#4B5563` / 캡션 `#6B7280` / 흐림 `#9CA3AF`
- 선 `#E5E7EB` (행·열 hairline) / 진한 선 `#D1D5DB` (헤더 밑줄·그룹 경계)
- 헤더 밴드 · 페이지 배경 `#F3F4F6` / 교차 배경 `#F9FAFB` / 비고 열 `#FAFBFC`
- 그라디언트·그림자·다른 색 추가 금지. 카드에는 그림자 없음.

**타이포 (크기를 키우거나 줄이지 마세요)**
- 폰트 Pretendard 하나만. 페이지 제목 20/700, 카드 제목 13~14/700, 표 열 헤더 12/500 `#6B7280`, 표 본문 13/400, 비고·캡션 12px, 배지 11px.
- 한국어 블록에는 항상 `word-break: keep-all` (없으면 어절이 한 글자씩 쪼개짐).
- 숫자 열은 `font-variant-numeric: tabular-nums` + 우측 정렬.

**표 (가장 중요)**
- `table-layout: fixed`, 열 너비는 **`<th>`의 inline `width`**로 지정. `<colgroup>/<col>`은 쓰지 마세요(제거되면 모든 열이 균등 분할됨).
- 표를 감싸는 요소에 `overflow`(auto/hidden 포함) 금지. overflow가 걸린 조상이 sticky 기준이 되어 헤더 고정이 깨집니다. 카드에도 `overflow:hidden` 금지.
- 열 헤더: `#F3F4F6` 배경, 하단 1px `#D1D5DB`, `position:sticky; top:0`.
- 셀: padding 7~8px 12~16px, `vertical-align: top`, 행 구분 1px `#E5E7EB`, **열 구분선도 1px `#E5E7EB`**(열이 4개 이상이면 필수).
- 그룹 병합은 `rowspan` + 그룹 열 배경 `#F9FAFB` + 그룹 경계 2px `#D1D5DB`.
- 값 안의 **요율·금액·코드는 파랑 볼드**(`#2563EB`/700), 그 외 볼드 금지.
- 코드값 목록은 칩으로: 흰 배경, 1px `#E5E7EB`, radius 5px, 12px, 코드만 파랑 볼드. gap 5px.
- 형식/구분값은 배지로: `#EFF6FF` 배경 파란 글씨(주 값), `#F3F4F6` 배경 회색 글씨(부가 값), 11px/600, radius 4px.
- 검색은 **행을 숨기지 말고 흐리게**(`opacity:.3`) 처리하고 일치 건수를 표시. rowspan이 무너집니다.

**필터 컨트롤**
- 첫 줄(대분류)은 **모서리 6px 사각 버튼**. 활성 = 파랑 `#2563EB` 배경 + 흰 글씨, 비활성 = 흰 배경 + 1px `#D1D5DB` + `#4B5563`.
- 둘째 줄(소분류)은 **pill(radius 999px)**. 활성 = 1px `#2563EB` 테두리 + `#EFF6FF` 배경 + `#2563EB` 글씨, 비활성 = 흰 배경 + 1px `#D1D5DB`.
- 화면에 "대분류"·"소분류"라는 글자를 넣지 마세요. 형태로만 구분합니다.
- 검색 입력 radius 8px, 좌측 14px 돋보기 아이콘 `#9CA3AF`, 우측에 일치 건수 12/600 파랑.

**레이아웃**
- 좌측 사이드바 216px(흰 배경, 우측 1px `#E5E7EB`), 메뉴 항목은 16px 아이콘 + 13px 라벨, 활성 항목은 `#EFF6FF` 배경 + 파란 글씨.
- 본문 컨테이너 좌우 24px, 카드 radius 10px + 1px `#E5E7EB`, 카드 간 16px.
- 아이콘은 16px, stroke 1.75, `currentColor`. 이모지 금지.

**내용 규칙**
- 표 안의 법령·고시 문구는 **한 글자도 수정·요약하지 마세요**. 없는 데이터를 추측해 채우지 마세요.
- 문장은 존댓말·평서문, 과장 표현 금지.

작업: (여기에 할 일을 적으세요. 예 — "특정내역 코드 조회 화면을 위 규칙대로 만들어줘. 열은 코드 / 명칭 / 기재요령 / 관련 서식이고 데이터는 첨부 파일에 있어.")

## 여기까지 복사

---

## 팀원에게 함께 주면 좋은 것
- `design_handoff_copay_table/` 폴더 (완성 화면 HTML + CSS + README). "이 폴더 스펙 그대로 재현해줘"가 가장 정확합니다.
- 참고 화면: `본인부담금 규칙.dc.html`, `SAM 파일 레이아웃.dc.html`

## 자주 어긋나는 지점 (검수 체크리스트)
1. 글씨 크기를 키웠는가 → 안 됨 (13px 본문 유지)
2. 파랑을 네이비/다른 톤으로 바꿨는가 → 안 됨 (`#2563EB`)
3. 열 너비를 `<colgroup>`에 넣었는가 → `<th>`로 옮기기
4. 표 감싸는 div에 `overflow` 넣었는가 → 제거
5. 대분류를 pill로, 소분류를 사각 버튼으로 바꿨는가 → 반대
6. 검색이 행을 숨기는가 → 흐리게로 변경
