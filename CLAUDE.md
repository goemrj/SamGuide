# SamGuide — Claude Code 프로젝트 가이드

심사청구 점검 로직을 짤 때 필요한 자료(본인부담금 규칙 · SAM 레이아웃 · 특정내역 · 산정특례 등)를
한 화면에서 찾아보기 위한 **로컬 전용 사이트**.

> 이 프로젝트는 가계부(OurBudget / dahyedonggubudget)와 **아무 관계가 없다.**
> 그쪽 규칙(Firestore · `save()` · viewport 680 · 단일 index.html)을 여기 적용하지 않는다.

---

## 먼저 읽을 것

**작업을 시작하기 전에 `README.md` 를 읽는다.** 폴더 구조, 카테고리별 상태,
데이터 파일을 원본(고시 PDF · 엑셀)에서 다시 만드는 방법이 전부 거기 있다.

---

## 성격

- 사용자는 개발자가 아니다. 비트컴퓨터 메디전트실에서 병원이 심평원으로 보내는 SAM 파일을
  점검하는 프로그램을 만든다. 이 사이트는 그 업무의 **참고자료 모음**이다.
- 화면에 적힌 값은 **심사 기준 문서**다. 틀리면 실제 청구가 틀어진다.
- 빌드 없음. 프레임워크 없음. `index.html` 을 더블클릭하면 열린다.
  주소로 열려면 `serve.ps1` (포트 8392).
- 외부로 아무것도 보내지 않는다. CDN·폰트·API 호출을 새로 넣지 않는다.

---

## 절대 하지 말 것

- **원본 데이터의 글자를 바꾸지 않는다.** 항목명 · 계산식 문자열 · 비고 문구 · 코드는
  고시·엑셀 원문 그대로 옮긴다. 줄이거나 의역하지 않는다.
  (열 이름처럼 화면 표기용으로 이미 정리한 것은 예외이고, 바꿀 때 README 에 남긴다.)
- 추측해서 값을 채우지 않는다. 원본에 없으면 **사용자에게 물어본다.**
- 사용자가 요청하지 않은 리팩터링·테마 변경을 하지 않는다.
- `js/layout-*.js` 를 이 폴더에서 고치지 않는다 — SamEditor 원본을 **복사**한 것이다.
  갱신은 다시 복사하는 방식으로만 한다.
- 커밋·푸시는 맨 아래 「깃 규칙」을 따른다.

---

## 소스 나누는 규칙

카테고리 하나에 파일 하나. 새 카테고리가 생기면 그때도 나눈다.

```
js/common.js          화면 전환 + 두 화면 이상이 쓰는 도우미만
js/page-<이름>.js     그 카테고리 화면 하나
data/<이름>.js        그 카테고리 데이터 (원본에서 생성)
tools/                원본 PDF·엑셀 → data/ 변환 스크립트
```

한 화면에서만 쓰는 코드를 `common.js` 에 넣지 않는다.
**메모장은 카테고리가 늘어도 사이드바 맨 아래에 둔다.**

---

## 디자인

- 색·글자 크기는 `css/style.css` 맨 위 `:root` 토큰이 **전부**다. 없는 색을 새로 만들지 않는다.
- 고정(sticky)하는 것: **사이드바** · **각 화면 맨 위 상단 바**(① 은 `.b2-top`, 나머지는 `.page-head`)
  · **표의 열 머리줄**. 머리줄은 상단 바 아래에 붙는다 — 상단 바 높이를 `common.js` 의
  `setStickTop()` 이 재서 `--stick` 으로 넘기고 `th{top:var(--stick)}` 가 그 값을 쓴다.
  (2026-08-20 — 원래 규칙은 "고정은 사이드바뿐"이었다.
  표 머리줄은 핸드오프 요구사항, 상단 바는 사용자 요청으로 뒤집었다. README 참조.)
- **좌우 스크롤을 만들지 않는다 — SamGuide 전체 전제.** 어떤 화면에서도 페이지에 가로
  스크롤바가 생기면 안 된다. 표가 넓어지면 표를 넓히지 말고 **칸 안에서 줄바꿈**시킨다.
  그래서 `white-space:nowrap` 은 표 칸·머리글자에 쉽게 걸지 않는다(값이 아주 짧을 때만).
- **모든 화면의 표는 같은 형식이다** — `table.b2`(①)와 `table.fields`(나머지)에 같은 규칙을 건다.
  머리줄 회색 띠 12px · 본문 13px · 칸 여백 8/16px · 행·열 구분선 1px. 계산기의 입력·결과 표
  (`.items`·`.res`)만 예외로 작게 둔다.
- 표는 **열을 균등분배하지 않는다.** 너비를 정한 표만 `table-layout:fixed` + `<th>` 인라인 `width`,
  나머지는 `table-layout:auto` + `width:100%`(열은 내용에 맞고 표는 카드를 넘지 않는다).
  **너비 없이 fixed 를 걸면 안 된다**(균등분배가 된다). `colgroup/col` 은 쓰지 않는다.
  표 자체에 스크롤을 두지 않는다.
- 열 너비는 **모든 화면의 표에서** 머리줄 경계를 끌어서 바꿀 수 있다(`common.js`).
  **두 열이 폭을 주고받아 표 전체 폭은 그대로다**
  (가로 스크롤 금지 전제). 손잡이를 두 번 누르면 처음 너비로 돌아간다. README 참조.
- 표·카드를 감싸는 요소에 `overflow` 를 주지 않는다 — sticky containing block 이 되어
  머리줄 고정이 깨지고, 특정기호 배지 툴팁도 잘린다.
- **필터는 어느 화면이든 두 줄 규칙을 따른다** (SAM 파일 레이아웃이 기준).
  - 대분류(첫 줄) = `.chips-seg` — 모서리 8px **네모 박스**, 고른 것은 파랑 채움 + 흰 글씨.
  - 소분류(둘째 줄) = `.chips-rec` — pill(999px), 고른 것은 **밝은 하이라이트**
    (`--accent-tint` 배경) + 파랑 테두리·글자.
  - 화면에 "대분류"·"소분류" 라는 글자는 넣지 않는다 — 모양으로만 구분한다.
  - 줄이 하나뿐인 화면(산정특례 등)은 그 줄을 대분류로 본다 → `.chips-seg`.
- 지브라 줄무늬 쓰지 않는다.
- Toss · Apple · Notion 결. 화이트 · 네이비 · 여백. 과한 색과 애니메이션은 쓰지 않는다.

---

## 이 PC의 환경 제약

- **한글(HWP) COM 자동화는 안 된다** — 한컴 보안 대화상자에서 멈춘다.
  hwp 자료가 필요하면 **사용자에게 한글에서 PDF로 저장해 달라고 요청**한다(쪽번호가 유지된다).
- **Excel COM · Word COM 은 정상 동작**한다. python 은 없다(Store 스텁만).
- PDF 는 Git for Windows 의 **xpdf `pdftotext`**. poppler 가 아니라서 `-bbox-layout` 이 없고,
  대신 **`-table`** 이 있다. 여러 단으로 된 고시 표는 `-layout` 말고 `-table` 로 뽑아야 칸이 안 섞인다.
- awk 로 칸을 자를 때 **`LC_ALL=C.UTF-8` 필수**. 없으면 글자를 바이트로 세어 위치가 어긋난다.
- **awk 를 줄번호(`NR==…`)로 편집하지 않는다.** 앞선 편집으로 번호가 밀려 엉뚱한 줄을 덮어쓴다.
  파일 수정은 Edit 도구로 한다.

---

## 작업 순서

요청 분석 → 기존 코드 확인 → 변경 계획 설명 → 구현 → **브라우저로 확인** → 변경 파일 안내.

값을 건드렸으면 원본(심평원 페이지·엑셀)과 대조한 결과를 함께 알린다.
계산기를 건드렸으면 세부작성요령(2025.8.1.) 203~206쪽 예시로 다시 검산한다.
총진료비 28,800원 · 진찰료 21,030원(부담률 100%) · 법정본인부담률 60%
→ 산정대상 7,770 × 60% = 4,662 + 21,030 = **본인부담 25,692원 / 청구 3,108원**.
(요령에 적힌 25,600원은 여기서 100원 미만을 버린 값이다 — 2026-08-21 부터 화면은 절사하지 않는다.)

---

## 깃 규칙

혼자 작업한다 (2026-08-21 — 예전의 팀 공동작업 · 담당 영역 규칙은 걷어냈다).
어떤 폴더든 물어보지 않고 고친다. 다만 값을 건드리는 규칙(위 「절대 하지 말 것」)은 그대로다.

- 파일 수정이 끝나면 `git add .` → `git commit` → `git push` 로 바로 올린다. 커밋 메시지는 한국어 한 줄.
- 작업 전 `git pull` 은 다른 PC에서 만졌을 수 있으니 그대로 한 번 돌린다(대개 "Already up to date").
- `git push --force` 계열은 쓰지 않는다.
- `git reset --hard`, `git checkout .` 같은 되돌리기 명령은 사용자 확인 없이 실행하지 않는다.
- `.env`, API 키, 개인정보가 담긴 파일은 커밋하지 않는다.
