/* ---------- 약국 산정특례 ----------
   아직 자료가 없어 화면(index.html #page-pharm)만 만들어 두었다.
   자료를 받으면 tools/ 에 변환 스크립트를 두고 data/pharm-codes.js 를 만든 뒤
   이 파일에서 그리면 된다 — 다른 카테고리와 같은 방식.
------------------------------------------------------------------ */
